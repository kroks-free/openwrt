#include "mt7628an_kroks.dtsi"
#include "flash_kroks_16m.dtsi"

#include <dt-bindings/input/input.h>
#include <dt-bindings/gpio/gpio.h>

/ {
	compatible = "kroks,kndrt31r6", "mediatek,mt7628an-soc";
	model = "Kroks KNdRt31R6";

	aliases {
		led-boot = &led_status;
		led-failsafe = &led_status;
		led-running = &led_status;
		led-upgrade = &led_status;
	};

	leds {
		compatible = "gpio-leds";

		led_status: status {
			label = "kndrt31r6:led:status";
			gpios = <&gpio0 2 GPIO_ACTIVE_LOW>;
		};

		wifi {
			label = "kndrt31r6:led:wifi";
			gpios = <&gpio1 12 GPIO_ACTIVE_LOW>;
			linux,default-trigger = "phy0tpt";
		};
	};

	keys {
		compatible = "gpio-keys";

		reset {
			label = "reset";
			gpios = <&gpio1 6 GPIO_ACTIVE_LOW>;
			linux,code = <KEY_RESTART>;
		};
	};
};

&esw {
	mediatek,portmap = <0x3e>;
};

&state_default {
	gpio {
		ralink,group = "i2s", "wdt", "wled_an";
		ralink,function = "gpio";
	};

	p0led_an {
		ralink,group = "p0led_an";
		ralink,function = "p0led_an";
	};

	p1led_an {
		ralink,group = "p1led_an";
		ralink,function = "p1led_an";
	};
};
